TODO: Do not generate TestCase dynamically, use

class TestCase(unittest.TestCase):
    ...


---

def test(self):
    with self.get_fixture('NAME') as res:
        1

----

1. class_scoped -> default_scopes = ['class']



generic way to enable / disable fixtures (cli)

--db sqlite,postgres --ipdb --log

----

if no wrappers, use ExitStack

---

lazy fixtures: make concept better